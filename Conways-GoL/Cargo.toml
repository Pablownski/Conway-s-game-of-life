[package]
name = "Conways-GoL"
version = "0.1.0"
edition = "2024"

[dependencies]
minifb = "0.27"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
